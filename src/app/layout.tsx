import { DefaultLayout } from "@/layouts/DefaultLayout";

const RootLayout = ({children}: Readonly<{children: React.ReactNode}>)=>  <DefaultLayout>{children}</DefaultLayout>

export default RootLayout
